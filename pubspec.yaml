name: ai_fitness_tracker
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.8.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: # state management
  bloc: any
  firebase_core: any # core firebase sdk
  cloud_firestore: any # firestore database sdk
  firebase_auth: any # firebase authentication sdk
  google_sign_in: ^7.1.1 # google sign-in sdk
  hive: any # local database sdk
  hive_flutter: any # hive flutter adapter
  sqflite_sqlcipher: ^3.3.0 # encrypted sqlite (SQLCipher)
  path: any # used for file path joins in SQLite helper
  path_provider: any # find paths in filesystem
  sensors_plus: any # access to device sensors
  intl: any # date/number formatting utilities
  # UI helpers
  lottie: ^3.3.1 # later, optional for animations
  flutter_secure_storage: any # keystore-backed secure storage for Hive encryption keys
  flutter_dotenv: ^5.1.0 # load env vars from .env during development
  dio: ^5.7.0 # robust HTTP client with interceptors and cancelation
  flutter_markdown: ^0.7.3 # render Markdown in chat bubbles
  connectivity_plus: ^6.0.5 # network connectivity status for retry UX

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  entry: ^1.0.2
  simple_icons: ^14.6.1
  icons_plus: ^5.0.0
  sign_in_with_apple: ^7.0.1
  horizontal_slider: ^0.0.3
  animated_weight_picker: ^1.0.3
  awesome_icons: ^1.1.0
  dynamic_multi_step_form: ^1.1.5
  msh_checkbox: ^2.0.1
  wheel_chooser: ^1.1.2
  easy_date_timeline: ^1.1.3
  flutter_floating_bottom_bar: ^1.3.0
  animated_notch_bottom_bar: ^1.0.3
  font_awesome_flutter: ^10.9.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.3
  bloc_test: ^9.1.7

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/welcome_background.png
    - assets/images/wc.jpeg
    - assets/images/wwc.png
    - assets/images/w.jpeg
    - assets/images/c.jpeg
    - assets/images/cc.jpeg
    - assets/images/_.jpeg
    - assets/images/dl1.jpg
    - assets/images/
    # Audit artifacts (for in-app viewing in the Audit Dashboard)
    - docs/audit-report.md
    - docs/findings.csv
    - docs/dfd.mmd
    - .github/workflows/security-audit.yml
    - .semgrep/semgrep.yml
    - scripts/run-audit.sh
  # Do NOT bundle environment files; set secrets via --dart-define at build time.
  # .env intentionally excluded from assets to avoid shipping secrets.
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: Sora
      fonts:
        - asset: assets/fonts/Sora-Thin.ttf
          weight: 100
        - asset: assets/fonts/Sora-ExtraLight.ttf
          weight: 200
        - asset: assets/fonts/Sora-Light.ttf
          weight: 300
        - asset: assets/fonts/Sora-Regular.ttf
          weight: 400
        - asset: assets/fonts/Sora-Medium.ttf
          weight: 500
        - asset: assets/fonts/Sora-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Sora-Bold.ttf
          weight: 700
        - asset: assets/fonts/Sora-ExtraBold.ttf
          weight: 800
        # Optional variable font (kept last; some platforms support variable axes)
        - asset: assets/fonts/Sora-VariableFont_wght.ttf
          weight: 400
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
